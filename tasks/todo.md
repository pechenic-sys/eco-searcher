# Serper + DeepSeek Search Skill

- [x] Define trigger phrases and evidence-first workflow.
- [x] Implement config loading with environment override and sanitized errors.
- [x] Implement Serper search, URL normalization, page bounds, retries, and DeepSeek JSON extraction.
- [x] Add Codex metadata and cross-platform installers.
- [x] Add user guide, API key locations, GitHub README, security policy, and token estimate.
- [ ] Publish to a new GitHub repository and verify the remote URL after the user supplies/creates it.

## Review

Validation must include syntax checks, unit tests, the bundled skill validator, and a repository-wide secret scan. Live API checks require the user's own keys and are not part of the repository publication.

