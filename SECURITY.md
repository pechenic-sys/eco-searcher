# Security

Do not commit `config.toml`, `.env`, API keys, bearer tokens, cookies, or private source content. Revoke and rotate a key immediately if it appears in a commit, terminal recording, issue, or pull request. The helper sends credentials only in HTTP headers and replaces provider details in user-facing errors with sanitized messages. Report a suspected repository vulnerability privately to the repository maintainer.

