# Copy to config.toml and fill in the two keys locally.
# config.toml is ignored by Git. Environment variables take precedence.
serper_api_key = ""
deepseek_api_key = ""

serper_endpoint = "https://google.serper.dev/search"
deepseek_base_url = "https://api.deepseek.com"
deepseek_model = "deepseek-v4-flash"
country = "ru"
language = "ru"
results_per_query = 10
max_sources = 8
max_page_chars = 12000
timeout_seconds = 30
retries = 2

